# Proyecto Texto a SQL con LangChain

Este proyecto convierte consultas en lenguaje natural a comandos SQL utilizando LangChain.
 
## Instalación

1. Clona el repositorio:
    ```bash
    git clone https://github.com/tu_usuario/langgraph-research-agent.git
    ```

2. Navega al directorio del proyecto:
    ```bash
    cd langgraph-research-agent
    ```

3. Instala las dependencias:
    ```bash
    pip install -r requirements.txt
    ```
## Configuración

Rellenar el archivo .env

## Uso

-- To Fill --

## Docker
Ejecutar desde la carpeta principal:
    ```
    docker compose up --build
    ```
En caso de querer eliminar el contenedor:
    ```
    docker-compose down
    ```
